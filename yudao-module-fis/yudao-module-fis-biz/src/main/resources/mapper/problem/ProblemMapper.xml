<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.fis.dal.mysql.problem.ProblemMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectProblemReportInfo"
            resultType="cn.iocoder.yudao.module.fis.controller.admin.problem.vo.ProblemReportRespVO">
        select issue.id,
        answer.answer_id,
        answer.answer_name,
        issue.problem_type,
        1 as problemSize,
        case
        when answer.answer_content
        !=
        '' then 1
        else 0 end as answerSize,
        case
        when answer.answer_score
        !=
        '' then 1
        else 0 end as commentSize,
        case
        when answer.answer_score
        !=
        '' then answer.answer_score
        else 0 end as answerCountSize
        ,
        issue.detail_id,
        date(issue.create_time) as createTime,
        concat('WK', week(issue.create_time)) as week,
        concat('M', month(issue.create_time)) as month
        from problem.problem_answer answer
        left join problem.problem_info issue
        on answer.problem_id = issue.id
        <where>
            <if test="username != null and username !=''">
                and issue.pub_work_id = #{username}
            </if>
            <if test="timeSpan !=null and timeSpan.length==2">
               and issue.create_time between #{timeSpan[0]} and #{timeSpan[1]}
            </if>
        </where>
    </select>
    <select id="selectAnswerReportInfo"
            resultType="cn.iocoder.yudao.module.fis.controller.admin.problem.vo.ProblemReportRespVO">
        select issue.id,
        answer.answer_id,
        answer.answer_name,
        issue.problem_type,
        1 as problemSize,
        case when answer.answer_content
        !=
        '' then 1 else 0 end as answerSize,
        case when answer.answer_score
        !=
        '' then 1 else 0 end as commentSize,
        case when answer.answer_score
        !=
        '' then answer.answer_score else 0 end as 回答评价总得分
        ,
        issue.detail_id,
        date(issue.create_time) as createTime,
        concat('WK', week(issue.create_time)) as week,
        concat('M', month(issue.create_time)) as month
        from problem.problem_answer answer
        left join problem.problem_info issue
        on answer.problem_id = issue.id
        <where>
            <if test="username != null and username !=''">
                and answer.answer_id = #{username}
            </if>
            <if test="timeSpan !=null and timeSpan.length==2">
                and issue.create_time between #{timeSpan[0]} and #{timeSpan[1]}
            </if>
        </where>
    </select>
</mapper>
